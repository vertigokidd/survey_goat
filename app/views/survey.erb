<div id="surveyWrapper">
  <h1><%= @survey.title %></h1>
  <form action="/create" method="POST">
    <ol>
      <% @survey.questions.each do |question| %>
      <li>
          <span><%= question.content %></span>
          <ul>
            <% question.answers.each do |answer| %>
              <li><label><input type="radio" name="<%= question.id %>"><%= answer.choice %></label></li>
            <% end %>
          </ul>
      </li>
      <% end %>
    </ol>
    <input type="submit" id="submitSurvey"value="Submit the survey">
  </form>

</div>
